#!/usr/bin/env bash
# macOS: double-click this file in the Finder to start MonopInk.
# (First time: right-click > Open, because the file is not signed.)
cd "$(dirname "$0")" && exec ./monopink.sh web
